<div class="jumbotron">
  <h1>Blocipedia</h1>
  <p>Blocipedia allows you to create wikis</p>
  <p>
    <% if current_user %>
      Hello! Welcome Back!
    <% else %>
      <%= link_to "Sign In", new_session_path %> or
      <%= link_to "Sign Up", new_user_path, class: 'btn btn-primary' %> today!
    <% end %>
  </p>
</div>

  <% if current_user && current_user.standard? %>
    <%= link_to "Upgrade?", new_charge_path, class: 'btn btn-primary' %>

  <% elsif current_user && current_user.premium? %>
    <%= link_to "Downgrade?", '', class: 'btn btn-primary' %>
  <% end %>
